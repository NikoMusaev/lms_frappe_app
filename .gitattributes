# Код исполняется в Linux-контейнерах — переводы строк только LF.
# Симптом нарушения: bash в контейнере сообщает «file not found» про
# существующий файл, потому что \r попал в шебанг.
* text=auto eol=lf
